---
description: Implement one bounded TeamMate feature with tests and documentation.
agent: build
---

Implement only this feature/scope: $ARGUMENTS

Before editing:
1. read AGENTS.md;
2. read docs/00_SOURCE_OF_TRUTH.md and docs/STATUS.md;
3. locate the relevant SRS requirements and roadmap phase;
4. inspect existing implementation and reuse established patterns.

Then:
- state the acceptance criteria you will satisfy;
- implement the smallest complete vertical slice;
- enforce server-side validation and authorization;
- add/update appropriate tests;
- run relevant lint/typecheck/tests/build checks;
- update documentation and docs/STATUS.md if behavior/status changed;
- review the diff for unrelated changes.

Do not commit or push.
Do not implement adjacent roadmap features unless strictly required for this feature to function.
