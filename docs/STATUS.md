# Project Status

## Current phase

**Phase 0 — Foundation**

Status: implementation complete (Checkpoints 1–3). All quality gates pass locally. First hosted CI run pending an approved push.

## Completed

Foundation and infrastructure:

- Next.js 16 App Router application scaffolded.
- Strict TypeScript with `noUncheckedIndexedAccess` and `allowJs` explicitly disabled.
- PostgreSQL + Redis Docker infrastructure; both containers carry healthchecks.
- Prisma 7 with the `@prisma/adapter-pg` PostgreSQL driver adapter.
- Server-only database boundary (server-only guard with a script-safe wrapper).
- `db:check` verifying a live PostgreSQL connection through Prisma.
- Phase 0 environment validation (`DATABASE_URL` required; `NODE_ENV` validated when set).
- ActionResult error/result convention.

Design and shell:

- Tailwind CSS 4 + shadcn/ui foundation (`components.json`, `cn` utility).
- Button and Card primitives only.
- Semantic light/`.dark` design tokens (class strategy, no theme provider yet).
- TeamMate landing/public shell with product metadata and disabled sign-in CTA.
- Branded 404 and global error pages.

Quality and tooling:

- Vitest + Testing Library setup (23 tests across 5 files).
- Prettier formatting gate (`format`, `format:check`, `.prettierignore`).
- `verify` script: format:check → lint → typecheck → test → build.
- npm lifecycle-script allowlist (pinned `allowScripts` entries).
- Prisma Client generated by `postinstall` (`prisma generate`).
- GitHub Actions CI configuration (`.github/workflows/ci.yml`) targeting `main`.
- Local branch renamed `master` → `main` (W7).

## Deferred / pending external

- CI workflow exists, but the first hosted GitHub run is pending: pushing is explicitly forbidden until user approval.
- Playwright E2E remains Phase 1: `test:e2e` is intentionally inactive until then.
- Protected app shell remains Phase 1.
- Project folders are created on first real use, not as empty speculative directories.
- `@hookform/resolvers` remains deferred due to the bootstrap peer-resolution conflict.
- No Prisma migration exists yet — by design; migrations arrive with their first reviewed schema change.

## In progress

None.

## Next target

Review Phase 0, then implement Phase 1 (authentication and onboarding).

## Open decisions

- Production managed PostgreSQL vendor.
- Production Redis provider.
- S3-compatible storage provider.
- Email sender/domain configuration.
- Final OpenAI model and quotas.

These decisions do not block Phase 0.

## Known risks

- Dependency/version changes must be checked against lockfile and current official docs before production.
- Auth/database schema generation must be reviewed rather than blindly accepted.
- AI matching must remain advisory and cost-limited.

## Last update

Checkpoint 3: branch renamed to `main`, CI workflow added, documentation synchronized, approved cleanup applied.
